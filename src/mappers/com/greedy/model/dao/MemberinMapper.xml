<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.greedy.member.MemberinMapper">
	
	<insert id="memberJoin" parameterType="com.greedy.DTO.MemberDTO">
		INSERT
		  INTO MEMBER A
		(
		  A.MEMBER_ID
		, A.MEMBER_PWD
		, A.MEMBER_NAME 
		)
		VALUES
		(
		  #{memberId}
		, #{memberPwd}
		, #{memberName}
		)
		  
	</insert>
	 
</mapper>
